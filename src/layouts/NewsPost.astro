---
import { Image } from 'astro:assets';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import '../styles/home.css'; 

type Props = {
  title: string;
  description?: string;
  pubDate: string;
  heroImage?: string;
};

const { title, description, pubDate, heroImage } = Astro.props;
---

<html lang="es">
  <head>
    <BaseHead title={title} description={description} />
    <style>
     /* --- Estilo tipo “ejemplo fino” para tu página de noticia --- */

main {
  width: calc(100% - 2em);
  max-width: 100%;
  margin: 0 auto;
  padding: 1rem;
  line-height: 1.7;
}

/* Contenedor de imagen estilo blog pro */
.hero {
  width: 100%;
  display: block;
  margin: 0 auto 1.2rem auto;
  border-radius: 12px;
  box-shadow: 0 3px 12px rgba(0,0,0,0.30);
  object-fit: cover;
}

/* Contenedor del texto principal */
.prose {
  width: 720px;
  max-width: calc(100% - 2em);
  margin: auto;
  padding: 1em;
  color: #4c4848;
}

h1 {
  margin-bottom: 0.8em;
  padding: 0.5em 0;
  text-align: center;
  line-height: 1.1;
  color: #4c4848;
}

.date {
  margin-bottom: 0.5em;
  color: #888;
  text-alig

    </style>
  </head>
  <body>
    <Header />
    <div class="arrow arrow-left">
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
    </div>

    <div class="arrow arrow-right">
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
      <div class="chevron"></div>
    </div>
    <!-- 🔼 Fin de las flechas -->


    <main>
      {heroImage && <Image width={1020} height={510} src={heroImage} alt="" class="hero" />}
      <h1>{title}</h1>
      <p class="date">
        <FormattedDate date={pubDate} />
      </p>
      <slot />
    </main>
    <Footer />
  </body>
</html>
